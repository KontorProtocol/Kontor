package kontor:contract;

world contract {
    include kontor:built-in/built-in;
    use kontor:built-in/context.{view-context, proc-context, signer};
    use kontor:built-in/error.{error};

    export init: func(ctx: borrow<proc-context>);

    export mint: func(ctx: borrow<proc-context>, n: string);
    export transfer: func(ctx: borrow<proc-context>, to: string, n: string) -> result<_, error>;
    export balance: func(ctx: borrow<view-context>, acc: string) -> option<string>;
    export balance-log10: func(ctx: borrow<view-context>, acc: string) -> option<string>;
}
