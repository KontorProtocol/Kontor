diff --git a/crates/wasmtime/src/runtime/component/func/options.rs b/crates/wasmtime/src/runtime/component/func/options.rs
--- a/crates/wasmtime/src/runtime/component/func/options.rs
+++ b/crates/wasmtime/src/runtime/component/func/options.rs
@@ -529,8 +529,13 @@
     }
 }

+/// A conversion exceeded the allocation allowance set by
+/// [`Store::set_hostcall_fuel`](crate::Store::set_hostcall_fuel).
+///
+/// This reports allowance exhaustion, not an allocation failure or exhaustion
+/// of WebAssembly instruction fuel.
 #[derive(Debug)]
-struct HostcallFuelExhausted;
+pub struct HostcallFuelExhausted;

 impl fmt::Display for HostcallFuelExhausted {
     fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
diff --git a/crates/wasmtime/src/runtime/component/func/typed.rs b/crates/wasmtime/src/runtime/component/func/typed.rs
--- a/crates/wasmtime/src/runtime/component/func/typed.rs
+++ b/crates/wasmtime/src/runtime/component/func/typed.rs
@@ -4,7 +4,7 @@
 use crate::component::storage::{storage_as_slice, storage_as_slice_mut};
 use crate::hash_map::HashMap;
 use crate::prelude::*;
-use crate::{AsContextMut, StoreContext, StoreContextMut, ValRaw};
+use crate::{AsContextMut, StoreContext, StoreContextMut, Trap, ValRaw};
 use alloc::borrow::Cow;
 use core::fmt;
 use core::hash::Hash;
@@ -1522,11 +1522,11 @@
         };
         debug_assert!(align.is_power_of_two());
         if ptr & (align - 1) != 0 {
-            bail!("string pointer not aligned to {align}");
+            bail!(Trap::UnalignedPointer);
         }
         match byte_len.and_then(|len| ptr.checked_add(len)) {
             Some(n) if n <= cx.memory().len() => cx.consume_fuel(n - ptr)?,
-            _ => bail!("string pointer/length out of bounds of memory"),
+            _ => bail!(Trap::StringOutOfBounds),
         }
         Ok(WasmStr {
             ptr,
@@ -1589,7 +1589,9 @@
         // Note that bounds-checking already happen in construction of `WasmStr`
         // so this is never expected to panic. This could theoretically be
         // unchecked indexing if we're feeling wild enough.
-        Ok(str::from_utf8(&memory[self.ptr..][..self.len])?.into())
+        Ok(str::from_utf8(&memory[self.ptr..][..self.len])
+            .map_err(|error| Error::from(error).context(InvalidStringEncoding))?
+            .into())
     }

     fn decode_utf16<'a>(&self, memory: &'a [u8], len: usize) -> Result<Cow<'a, str>> {
@@ -1598,7 +1600,8 @@
         debug_assert!(rest.is_empty());
         Ok(
             core::char::decode_utf16(chunks.iter().map(|chunk| u16::from_le_bytes(*chunk)))
-                .collect::<Result<String, _>>()?
+                .collect::<Result<String, _>>()
+                .map_err(|error| Error::from(error).context(InvalidStringEncoding))?
                 .into(),
         )
     }
@@ -3112,3 +3115,18 @@
     // a tradeoff of propagating a lot of unsafety, so it may not be worth it.
     panic!("bad type information detected");
 }
+
+/// A guest string is not valid in its canonical ABI encoding.
+///
+/// The underlying decoding error is retained as a source. This does not report
+/// allocation failures or errors returned by host functions.
+#[derive(Debug)]
+pub struct InvalidStringEncoding;
+
+impl fmt::Display for InvalidStringEncoding {
+    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
+        f.write_str("invalid canonical ABI string encoding")
+    }
+}
+
+impl core::error::Error for InvalidStringEncoding {}
diff --git a/crates/wasmtime/src/runtime/component/mod.rs b/crates/wasmtime/src/runtime/component/mod.rs
--- a/crates/wasmtime/src/runtime/component/mod.rs
+++ b/crates/wasmtime/src/runtime/component/mod.rs
@@ -126,7 +126,8 @@
     VMComponentAsyncStore, VecBuffer, WriteBuffer,
 };
 pub use self::func::{
-    ComponentNamedList, ComponentType, Func, Lift, Lower, TypedFunc, WasmList, WasmStr,
+    ComponentNamedList, ComponentType, Func, HostcallFuelExhausted, InvalidStringEncoding, Lift,
+    Lower, TypedFunc, WasmList, WasmStr,
 };
 pub use self::has_data::*;
 pub use self::instance::{Instance, InstancePre};
