package root:component;

world root {
    include kontor:built-in/built-in;
    use kontor:built-in/context.{view-context};

    record bad-record {
        names: list<string>,
    }

    export get-bad: async func(ctx: borrow<view-context>) -> bad-record;
}
