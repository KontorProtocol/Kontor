testlib::regtest_tests! {
    amm_contract,
    counter_contract,
    crypto_contract,
    fib_contract,
    native_token_contract,
    pool_contract,
    shared_account_contract,
    token_contract,
    wit_contract,
}
