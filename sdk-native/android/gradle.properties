# Fallback defaults for the KontorMobile_* properties build.gradle reads
# via getExtOrDefault/getExtOrIntegerDefault when the consuming app's
# rootProject.ext does not define them. NDK version matches the r27 pin
# used by CI (.github/workflows/mobile.yml) and the README.
KontorMobile_kotlinVersion=2.0.21
KontorMobile_minSdkVersion=24
KontorMobile_targetSdkVersion=34
KontorMobile_compileSdkVersion=35
KontorMobile_ndkVersion=27.1.12297006
