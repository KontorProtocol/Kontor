<!-- Generated by uniffi-bindgen-react-native -->
<!-- Used on AGP >= 7.3, where the namespace comes from build.gradle's
     `namespace` property and a manifest `package=` attribute is rejected.
     AndroidManifest.xml (with package=) remains for older AGP. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
</manifest>
