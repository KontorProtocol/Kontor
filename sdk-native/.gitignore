# Native build artifacts (produced by build-mobile.sh / cargo)
ios/*.xcframework
android/src/main/jniLibs/
**/build/
**/Pods/
*.a
*.so
*.dylib

node_modules/

# NOTE: the uniffi-bindgen output (src/generated/, cpp/, the iOS/Android
# glue, index.tsx/index.web.ts) IS committed for now so the package is
# self-contained and reviewable. It is regenerated by `npm run ubrn:generate`
# (see README). Revisit whether to gitignore it before merging.
